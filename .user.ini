; PL Exprés — proxy.php (9-sep-2026).
; PHP no debe desmontar los multipart de las subidas: el proxy reenvía el cuerpo
; crudo a Render, con el boundary original. Con esto apagado, $_POST y $_FILES
; quedan vacíos en TODO PHP de esta carpeta; el único PHP del sitio es proxy.php,
; que lee php://input y no los usa. Si algún día se añade otro PHP con formularios,
; hay que moverlo a otra carpeta o quitar esta línea.
enable_post_data_reading = Off
